diff -ruN e1/drivers/crypto/inside-secure/eip93/eip93-aead.c e2/drivers/crypto/inside-secure/eip93/eip93-aead.c
--- e1/drivers/crypto/inside-secure/eip93/eip93-aead.c	2026-02-16 16:36:10.029450000 +0100
+++ e2/drivers/crypto/inside-secure/eip93/eip93-aead.c	2026-02-16 21:03:13.959527500 +0100
@@ -136,6 +136,6 @@
 	struct crypto_aes_ctx aes;
 	struct sa_record *sa_record = ctx->sa_record_out;
 	u32 nonce = 0;
-	int ret;
+	int ret = -EINVAL;
 
 	if (crypto_authenc_extractkeys(&keys, key, len))
 		return -EINVAL;
@@ -153,26 +153,18 @@
 	switch ((ctx->flags & EIP93_ALG_MASK)) {
 	case EIP93_ALG_DES:
 		ret = verify_aead_des_key(ctfm, keys.enckey, keys.enckeylen);
-		if (ret)
-			return ret;
-
 		break;
 	case EIP93_ALG_3DES:
 		if (keys.enckeylen != DES3_EDE_KEY_SIZE)
 			return -EINVAL;
 
 		ret = verify_aead_des3_key(ctfm, keys.enckey, keys.enckeylen);
-		if (ret)
-			return ret;
-
 		break;
 	case EIP93_ALG_AES:
 		ret = aes_expandkey(&aes, keys.enckey, keys.enckeylen);
-		if (ret)
-			return ret;
-
-		break;
 	}
+	if (ret)
+		return ret;
 
 	ctx->blksize = crypto_aead_blocksize(ctfm);
 	dma_sync_single_for_cpu(ctx->eip93->dev, ctx->sa_record_base_in,
